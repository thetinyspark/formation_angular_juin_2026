import { CatalogFilterPipe } from './catalog-filter.pipe';

describe('CatalogFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
